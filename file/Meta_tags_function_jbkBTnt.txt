    elif call == "meta_tags":
        filters = {'lang': lang, 'page': form_data['page'], 'service_name': form_data['service_name']}

        data2 = frappe.db.sql("""
                   SELECT
                       trans.key , trans.value
                   FROM `tabMetaTags` tags
                       INNER JOIN `tabMeta Tags Language` trans
                           ON tags.name = trans.parent
                   WHERE trans.language = %(lang)s 
                   and 
                   tags.page = %(page)s 
                   and 
                   tags.service_name = %(service_name)s
                   """, filters, as_dict=True)

        frappe.local.response.update({"data": data2})

url 
https://apidev.variiance.com/api/staging/sendApi

data

{   
    "url": "profile/en/meta_tags",
    "data":{
        "service_name": "VLC",
        "page": "Home"
    }
}